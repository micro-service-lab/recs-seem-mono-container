package store_test

import "testing"

func TestGetCursorData(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name string
	}{
		{
			name: "success",
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
		})
	}
}
